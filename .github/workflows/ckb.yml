# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║  CKB • Code Intelligence for Pull Requests                                   ║
# ║  ─────────────────────────────────────────────────────────────────────────── ║
# ║  Risk Analysis • Complexity Gates • Coupling Detection • Smart Reviews       ║
# ╚══════════════════════════════════════════════════════════════════════════════╝

name: CKB

on:
  pull_request:
    branches: [develop, main]
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      force_index:
        description: 'Force full reindex'
        type: boolean
        default: false

concurrency:
  group: ckb-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write

env:
  CKB_TIER: standard
  THRESHOLD_CYCLOMATIC: 15
  THRESHOLD_COGNITIVE: 20
  THRESHOLD_COUPLING: 0.7
  THRESHOLD_DOC_COVERAGE: 70

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
          cache: true

      - name: Cache
        id: cache
        uses: actions/cache@v4
        with:
          path: .ckb/
          key: ckb-${{ runner.os }}-${{ github.base_ref }}-${{ hashFiles('go.sum') }}
          restore-keys: ckb-${{ runner.os }}-

      - name: Build
        run: go build -ldflags="-s -w" -o ckb ./cmd/ckb

      - name: Index
        id: index
        run: |
          ./ckb init
          START=$(date +%s)
          if [ "${{ inputs.force_index }}" = "true" ]; then
            ./ckb index --force 2>/dev/null || true
            echo "mode=full" >> $GITHUB_OUTPUT
          else
            ./ckb index 2>/dev/null || true
            echo "mode=incremental" >> $GITHUB_OUTPUT
          fi
          END=$(date +%s)
          echo "duration=$((END-START))" >> $GITHUB_OUTPUT

      - name: Analyze
        id: analyze
        run: |
          ./ckb pr-summary --base=origin/${{ github.base_ref }} --format=json > analysis.json 2>/dev/null || echo '{}' > analysis.json
          
          # Complexity
          echo '[]' > complexity.json
          for f in $(git diff --name-only origin/${{ github.base_ref }}...HEAD | grep -E '\.(go|ts|js|py)$' | head -15); do
            [ -f "$f" ] && ./ckb complexity "$f" --format=json 2>/dev/null | jq --arg f "$f" '. + {file: $f}' >> complexity.tmp || true
          done
          [ -f complexity.tmp ] && jq -s '.' complexity.tmp > complexity.json 2>/dev/null || echo '[]' > complexity.json
          
          # Coupling
          CHANGED=$(git diff --name-only origin/${{ github.base_ref }}...HEAD | grep -E '\.(go|ts|js|py)$' | tr '\n' ',' | sed 's/,$//')
          [ -n "$CHANGED" ] && ./ckb coupling --files="$CHANGED" --min-correlation=${{ env.THRESHOLD_COUPLING }} --format=json > coupling.json 2>/dev/null || echo '{}' > coupling.json
          
          # Contracts
          echo '{"changed":false}' > contracts.json
          CONTRACTS=$(git diff --name-only origin/${{ github.base_ref }}...HEAD | grep -E '\.(proto|graphql)$' | grep -E '(proto/|api/|graphql/)' || true)
          [ -n "$CONTRACTS" ] && echo "{\"changed\":true,\"files\":$(echo "$CONTRACTS" | jq -R -s 'split("\n") | map(select(length > 0))')}" > contracts.json
          
          # Audit & Docs
          ./ckb audit --min-score=60 --limit=10 --quick-wins --format=json > audit.json 2>/dev/null || echo '{}' > audit.json
          ./ckb docs index 2>/dev/null; ./ckb docs coverage --format=json > docs.json 2>/dev/null || echo '{}' > docs.json
          ./ckb docs stale --all --format=json > stale.json 2>/dev/null || echo '{}' > stale.json
          
          # Dead code & Ownership
          ./ckb telemetry dead-code --threshold=0.9 --limit=5 --format=json > deadcode.json 2>/dev/null || echo '{}' > deadcode.json
          ./ckb ownership drift --threshold=0.3 --limit=5 --format=json > drift.json 2>/dev/null || echo '[]' > drift.json

      - name: Comment
        uses: actions/github-script@v7
        env:
          CACHE_HIT: ${{ steps.cache.outputs.cache-hit == 'true' }}
          INDEX_MODE: ${{ steps.index.outputs.mode }}
          INDEX_TIME: ${{ steps.index.outputs.duration }}
        with:
          script: |
            const fs = require('fs');
            const read = (f, d) => { try { return JSON.parse(fs.readFileSync(f)); } catch { return d; } };
            
            const pr = read('analysis.json', {});
            const complexity = read('complexity.json', []);
            const coupling = read('coupling.json', {});
            const contracts = read('contracts.json', {});
            const audit = read('audit.json', {});
            const docs = read('docs.json', {});
            const stale = read('stale.json', {});
            const deadcode = read('deadcode.json', {});
            const drift = read('drift.json', []);
            
            const s = pr.summary || {};
            const risk = pr.riskAssessment || {};
            const reviewers = pr.suggestedReviewers || [];
            const modules = pr.modulesAffected || [];
            const files = pr.changedFiles || [];
            const couplingWarnings = coupling.missingCoupled || [];
            const auditItems = audit.items || [];
            const quickWins = audit.quickWins || [];
            const deadCandidates = deadcode.candidates || [];
            const staleCount = stale.totalStale || 0;
            
            // Complexity violations
            const complexViolations = complexity.filter(c => 
              (c.cyclomatic || 0) > ${{ env.THRESHOLD_CYCLOMATIC }} || 
              (c.cognitive || 0) > ${{ env.THRESHOLD_COGNITIVE }}
            );
            
            // Hotspots
            const hotspots = files.filter(f => f.isHotspot);
            
            // Risk items
            const criticalItems = auditItems.filter(i => i.riskLevel === 'critical');
            const highItems = auditItems.filter(i => i.riskLevel === 'high');
            
            // ════════════════════════════════════════════════════════════════
            // BUILD THE COMMENT
            // ════════════════════════════════════════════════════════════════
            
            const riskEmoji = { high: '🔴', medium: '🟡', low: '🟢' }[risk.level] || '⚪';
            const riskColor = { high: 'B91C1C', medium: 'D97706', low: '059669' }[risk.level] || '6B7280';
            const score = Math.round((risk.score || 0) * 100);
            
            let c = [];
            
            // ──────────────────────────────────────────────────────────────────
            // HEADER - Clean, scannable at a glance
            // ──────────────────────────────────────────────────────────────────
            c.push('<!-- ckb -->');
            c.push('');
            c.push(`## ${riskEmoji} CKB Report`);
            c.push('');
            
            // Metrics bar - the most important info in one line
            const metricsBar = [
              `**${s.totalFiles || 0}** files`,
              `**+${s.totalAdditions || 0}** / **-${s.totalDeletions || 0}**`,
              `**${s.totalModules || 0}** modules`,
              `**${s.hotspotsTouched || 0}** hotspots`
            ].join(' · ');
            
            c.push(`> ${metricsBar}`);
            c.push('');
            
            // ──────────────────────────────────────────────────────────────────
            // COVERAGE BAR - Visual risk indicator (inspired by Codecov)
            // ──────────────────────────────────────────────────────────────────
            const barWidth = 20;
            const filled = Math.round((1 - (risk.score || 0)) * barWidth);
            const empty = barWidth - filled;
            const safetyBar = '🟩'.repeat(Math.max(0, filled)) + '⬜'.repeat(Math.max(0, empty));
            
            c.push(`| Risk | ${safetyBar} | **${100 - score}%** safe |`);
            c.push('|:-----|:-----|:-----|');
            c.push('');
            
            // ──────────────────────────────────────────────────────────────────
            // ISSUES FOUND - Only if there are problems (clean when green)
            // ──────────────────────────────────────────────────────────────────
            const issues = [];
            if (criticalItems.length) issues.push(`🔴 **${criticalItems.length}** critical`);
            if (highItems.length) issues.push(`🟠 **${highItems.length}** high risk`);
            if (hotspots.length) issues.push(`🔥 **${hotspots.length}** hotspots`);
            if (complexViolations.length) issues.push(`📊 **${complexViolations.length}** complex`);
            if (couplingWarnings.length) issues.push(`🔗 **${couplingWarnings.length}** coupled`);
            if (contracts.changed) issues.push(`📜 contracts changed`);
            if (staleCount) issues.push(`📚 **${staleCount}** stale docs`);
            if (deadCandidates.length) issues.push(`💀 **${deadCandidates.length}** dead code`);
            
            if (issues.length > 0) {
              c.push(`> ⚠️ ${issues.join(' · ')}`);
              c.push('');
            } else {
              c.push('> ✅ **All checks passed** — No issues detected');
              c.push('');
            }
            
            // ──────────────────────────────────────────────────────────────────
            // REVIEWERS - Prominent when available
            // ──────────────────────────────────────────────────────────────────
            if (reviewers.length > 0) {
              const revList = reviewers.slice(0, 3).map(r => {
                const name = r.owner.startsWith('@') ? r.owner : `@${r.owner}`;
                const pct = Math.round((r.coverage || 0) * 100);
                return `${name} \`${pct}%\``;
              }).join(' · ');
              c.push(`**Suggested reviewers:** ${revList}`);
              c.push('');
            }
            
            // ──────────────────────────────────────────────────────────────────
            // RISK FACTORS - If any
            // ──────────────────────────────────────────────────────────────────
            if ((risk.factors || []).length > 0) {
              c.push('<details>');
              c.push('<summary>📋 Risk factors</summary>');
              c.push('');
              c.push('| Factor |');
              c.push('|:-------|');
              risk.factors.forEach(f => c.push(`| ${f} |`));
              c.push('');
              c.push('</details>');
              c.push('');
            }
            
            // ──────────────────────────────────────────────────────────────────
            // CRITICAL/HIGH RISK FILES
            // ──────────────────────────────────────────────────────────────────
            if (criticalItems.length + highItems.length > 0) {
              c.push('<details>');
              c.push(`<summary>⚠️ Risk audit · ${criticalItems.length} critical · ${highItems.length} high</summary>`);
              c.push('');
              c.push('| | File | Score | Factor |');
              c.push('|:-:|:-----|------:|:-------|');
              [...criticalItems, ...highItems].slice(0, 6).forEach(item => {
                const icon = item.riskLevel === 'critical' ? '🔴' : '🟠';
                const factor = (item.factors || [])[0]?.factor || '';
                c.push(`| ${icon} | \`${item.file}\` | ${item.riskScore} | ${factor} |`);
              });
              c.push('');
              c.push('</details>');
              c.push('');
            }
            
            // ──────────────────────────────────────────────────────────────────
            // COMPLEXITY
            // ──────────────────────────────────────────────────────────────────
            if (complexViolations.length > 0) {
              c.push('<details>');
              c.push(`<summary>📊 Complexity · ${complexViolations.length} violations</summary>`);
              c.push('');
              c.push('| File | Cyclomatic | Cognitive |');
              c.push('|:-----|----------:|----------:|');
              complexViolations.slice(0, 5).forEach(v => {
                const cyc = v.cyclomatic || 0;
                const cog = v.cognitive || 0;
                const cycIcon = cyc > ${{ env.THRESHOLD_CYCLOMATIC }} ? '⚠️' : '';
                const cogIcon = cog > ${{ env.THRESHOLD_COGNITIVE }} ? '⚠️' : '';
                c.push(`| \`${v.file}\` | ${cycIcon} ${cyc} | ${cogIcon} ${cog} |`);
              });
              c.push('');
              c.push(`> Thresholds: cyclomatic ≤ ${{ env.THRESHOLD_CYCLOMATIC }}, cognitive ≤ ${{ env.THRESHOLD_COGNITIVE }}`);
              c.push('');
              c.push('</details>');
              c.push('');
            }
            
            // ──────────────────────────────────────────────────────────────────
            // COUPLING
            // ──────────────────────────────────────────────────────────────────
            if (couplingWarnings.length > 0) {
              c.push('<details>');
              c.push(`<summary>🔗 Coupling · ${couplingWarnings.length} files often change together</summary>`);
              c.push('');
              c.push('| Missing | Usually changes with | Correlation |');
              c.push('|:--------|:---------------------|------------:|');
              couplingWarnings.slice(0, 5).forEach(w => {
                const pct = Math.round((w.correlation || w.couplingScore || 0) * 100);
                c.push(`| \`${w.file}\` | \`${w.coupledTo}\` | ${pct}% |`);
              });
              c.push('');
              c.push('</details>');
              c.push('');
            }
            
            // ──────────────────────────────────────────────────────────────────
            // HOTSPOTS
            // ──────────────────────────────────────────────────────────────────
            if (hotspots.length > 0) {
              c.push('<details>');
              c.push(`<summary>🔥 Hotspots · ${hotspots.length} volatile files</summary>`);
              c.push('');
              c.push('| File | Churn |');
              c.push('|:-----|------:|');
              hotspots.slice(0, 5).forEach(h => {
                const score = (h.hotspotScore || 0).toFixed(2);
                c.push(`| \`${h.path}\` | ${score} |`);
              });
              c.push('');
              c.push('</details>');
              c.push('');
            }
            
            // ──────────────────────────────────────────────────────────────────
            // CONTRACTS
            // ──────────────────────────────────────────────────────────────────
            if (contracts.changed && (contracts.files || []).length > 0) {
              c.push('<details>');
              c.push(`<summary>📜 Contract changes · ${contracts.files.length} files</summary>`);
              c.push('');
              contracts.files.forEach(f => c.push(`- \`${f}\``));
              c.push('');
              c.push('> ⚠️ Verify downstream consumers are compatible');
              c.push('');
              c.push('</details>');
              c.push('');
            }
            
            // ──────────────────────────────────────────────────────────────────
            // QUICK WINS
            // ──────────────────────────────────────────────────────────────────
            if (quickWins.length > 0) {
              c.push('<details>');
              c.push(`<summary>💡 Quick wins · ${quickWins.length} improvements</summary>`);
              c.push('');
              quickWins.slice(0, 4).forEach(w => {
                const effort = { low: '🟢', medium: '🟡', high: '🔴' }[w.effort] || '⚪';
                c.push(`- ${effort} **${w.action}** → \`${w.target}\``);
              });
              c.push('');
              c.push('</details>');
              c.push('');
            }
            
            // ──────────────────────────────────────────────────────────────────
            // MODULES - Collapsible, shows affected areas
            // ──────────────────────────────────────────────────────────────────
            const riskyModules = modules.filter(m => m.riskLevel === 'high' || m.riskLevel === 'medium');
            if (riskyModules.length > 0) {
              c.push('<details>');
              c.push(`<summary>📦 Modules · ${riskyModules.length} at risk</summary>`);
              c.push('');
              c.push('| Module | Files | Risk |');
              c.push('|:-------|------:|:----:|');
              riskyModules.slice(0, 5).forEach(m => {
                const icon = m.riskLevel === 'high' ? '🔴' : '🟡';
                c.push(`| \`${m.moduleId}\` | ${m.filesChanged} | ${icon} |`);
              });
              c.push('');
              c.push('</details>');
              c.push('');
            }
            
            // ──────────────────────────────────────────────────────────────────
            // DEAD CODE
            // ──────────────────────────────────────────────────────────────────
            if (deadCandidates.length > 0) {
              c.push('<details>');
              c.push(`<summary>💀 Dead code · ${deadCandidates.length} candidates</summary>`);
              c.push('');
              c.push('| Symbol | Confidence |');
              c.push('|:-------|:-----------|');
              deadCandidates.slice(0, 4).forEach(d => {
                const pct = Math.round((d.confidence || 0) * 100);
                c.push(`| \`${d.name}\` | ${pct}% |`);
              });
              c.push('');
              c.push('</details>');
              c.push('');
            }
            
            // ──────────────────────────────────────────────────────────────────
            // STALE DOCS
            // ──────────────────────────────────────────────────────────────────
            if (staleCount > 0) {
              c.push('<details>');
              c.push(`<summary>📚 Stale docs · ${staleCount} broken refs</summary>`);
              c.push('');
              (stale.reports || []).slice(0, 3).forEach(r => {
                (r.stale || []).slice(0, 2).forEach(s => {
                  c.push(`- \`${r.docPath}:${s.line}\` → ${s.rawText}`);
                });
              });
              c.push('');
              c.push('</details>');
              c.push('');
            }
            
            // ──────────────────────────────────────────────────────────────────
            // OWNERSHIP DRIFT
            // ──────────────────────────────────────────────────────────────────
            if (Array.isArray(drift) && drift.length > 0) {
              c.push('<details>');
              c.push(`<summary>👤 Ownership drift · ${drift.length} files</summary>`);
              c.push('');
              c.push('| File | Declared | Actual |');
              c.push('|:-----|:---------|:-------|');
              drift.slice(0, 4).forEach(d => {
                c.push(`| \`${d.path}\` | ${d.declaredOwner || '—'} | ${d.actualOwner || '—'} |`);
              });
              c.push('');
              c.push('</details>');
              c.push('');
            }
            
            // ──────────────────────────────────────────────────────────────────
            // FOOTER - Minimal, clean
            // ──────────────────────────────────────────────────────────────────
            c.push('---');
            const cacheStatus = process.env.CACHE_HIT === 'true' ? '💾' : '🔄';
            const indexMode = process.env.INDEX_MODE === 'incremental' ? '⚡' : '🔨';
            const indexTime = process.env.INDEX_TIME || '?';
            c.push(`<sub>${cacheStatus} ${indexMode} ${indexTime}s · [CKB](https://github.com/SimplyLiz/CodeMCP)</sub>`);
            
            // ════════════════════════════════════════════════════════════════
            // POST/UPDATE COMMENT
            // ════════════════════════════════════════════════════════════════
            const body = c.join('\n');
            
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            
            const existing = comments.find(c => c.user.type === 'Bot' && c.body.includes('<!-- ckb -->'));
            
            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body
              });
            }

      - name: Request Reviewers
        if: always()
        continue-on-error: true
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            try {
              const data = JSON.parse(fs.readFileSync('analysis.json'));
              const reviewers = (data.suggestedReviewers || [])
                .map(r => r.owner)
                .filter(r => !r.startsWith('@') && r !== context.actor)
                .slice(0, 2);
              
              if (reviewers.length) {
                await github.rest.pulls.requestReviewers({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  pull_number: context.issue.number,
                  reviewers
                });
              }
            } catch {}

      - name: Save Cache
        if: always()
        uses: actions/cache/save@v4
        with:
          path: .ckb/
          key: ckb-${{ runner.os }}-${{ github.base_ref }}-${{ hashFiles('go.sum') }}

      - name: Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ckb
          path: "*.json"
          retention-days: 7

  # ════════════════════════════════════════════════════════════════════════════
  # NIGHTLY REFRESH
  # ════════════════════════════════════════════════════════════════════════════
  refresh:
    name: Refresh
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
          cache: true

      - name: Build & Refresh
        run: |
          go build -ldflags="-s -w" -o ckb ./cmd/ckb
          ./ckb init
          ./ckb index --force
          ./ckb refresh-architecture --scope=all 2>/dev/null || true
          
          mkdir -p reports
          ./ckb hotspots --limit=50 --format=json > reports/hotspots.json 2>/dev/null || true
          ./ckb audit --min-score=40 --limit=50 --quick-wins --format=json > reports/audit.json 2>/dev/null || true
          ./ckb docs coverage --format=json > reports/docs.json 2>/dev/null || true
          
          echo "## 📊 CKB Nightly Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Metric | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Hotspots | $(jq 'length' reports/hotspots.json 2>/dev/null || echo 0) |" >> $GITHUB_STEP_SUMMARY
          echo "| Critical | $(jq '.summary.critical // 0' reports/audit.json 2>/dev/null || echo 0) |" >> $GITHUB_STEP_SUMMARY
          echo "| High Risk | $(jq '.summary.high // 0' reports/audit.json 2>/dev/null || echo 0) |" >> $GITHUB_STEP_SUMMARY
          echo "| Doc Coverage | $(jq '.coverage // 0' reports/docs.json 2>/dev/null || echo 0)% |" >> $GITHUB_STEP_SUMMARY

      - uses: actions/cache/save@v4
        with:
          path: .ckb/
          key: ckb-${{ runner.os }}-main-nightly

      - uses: actions/upload-artifact@v4
        with:
          name: ckb-nightly
          path: reports/
          retention-days: 30