# Smithery configuration file: https://smithery.ai/docs/config#smitheryyaml

startCommand:
  type: stdio
  commandFunction:
    |-
    config => ({ command: 'npx', args: ['-y', '@tastehub/ckb', 'mcp'] })
