[project]
name = "incremental-test"
version = "1.0.0"
description = "Test fixture for incremental indexing"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
